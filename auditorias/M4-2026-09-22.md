# Relatório de Auditoria Final — Módulo M4: Certificados e Horas Complementares

Auditoria somente leitura. Fonte funcional: `specs/M4-certificados.md`. Nenhum arquivo foi alterado; nenhuma correção foi implementada; nenhum teste foi criado; nenhum comando `git add`/`commit`/`push` foi executado. O documento `requisitos-envolvidos.md` não foi consultado.

---

## Seção 1 — Escopo e fontes

- **Módulo auditado:** M4 — Certificados e Horas Complementares (emissão, listagem, extrato de horas e verificação pública de certificados).
- **Fontes lidas integralmente:**
  - `specs/M4-certificados.md` — 18 regras (R1–R18), 26 critérios de aceite (§6), escopo e §2 "fora de escopo".
  - `entrevistas/M4-certificados.md` — tabela `# | Pergunta | Resposta | Fonte`: P1–P15 respondidas nas rodadas 2 / complementares (linhas 18–22, 37–39, 56–61), plus RN-401…RN-413; **nenhuma resposta final `PENDENTE`**.
  - `contrato-api.md` — seção HTTP do M4 (`POST /atividades/:id/certificado`, `GET /certificados`, `GET /extrato`, `GET /certificados/:codigo`).
  - `.opencode/skills/tdd-m4/SKILL.md` — R1–R18 confirmadas, 4 fatias (Fatia 1 = R1, R2, R12, R9).
  - `projeto.json` — `testes` contém apenas `npm --prefix api test` e `npm --prefix app test` (typecheck **não** está no `projeto.json`).
  - `AGENTS.md` — regras técnicas globais (relógio central, `X-Usuario`, testes de interface com API falsa, rotas `/_teste/*` só com `MODO_TESTE=1`).
- **Mapeamento de origem:** todas as regras R1–R18 citam P-01…P-15 da entrevista, que existem e têm resposta (a spec escreve `P-01`…`P-15`; a entrevista numera `P1`…`P15` — mesma pergunta, diferença apenas de formatação). Nenhuma regra sem origem, nenhuma origem pendente.
- **Limitação respeitada:** o documento externo de requisitos não foi procurado nem aberto.

---

## Seção 2 — Matriz de rastreabilidade (R1–R18)

| Regra | Origem | Teste que comprova | Veredito |
|---|---|---|---|
| R1 | P-01 (RN-404) | `api/tests/certificado.test.ts:82` «75% exato 3/4 → 201»; `:98` «50% 2/4 → 422»; `:112` «3/5 = 60%, sem arredondamento → 422»; `:126` «4/5 = 80% → 201» (critérios 1–4) | **COMPROVADA** |
| R2 | P-02 (RN-401) | `api/tests/certificado.test.ts:144` «422 `ATIVIDADE_NAO_ENCERRADA` antes do término»; `:158` «201 no instante exato do fim» (critérios 5–6) | **COMPROVADA** |
| R3 | P-03 (RN-411) | `api/tests/extrato.test.ts:392` «brutos 300/500, total 800, aproveitado 740» (critério 7); `api/tests/horas-complementares.test.ts:45` «90 mantém integral» (critério 10) | **COMPROVADA** |
| R4 | P-03 (RN-411) | `api/tests/extrato.test.ts:445` «palestrasMinutos 300 bruto e aproveitado 240»; `api/tests/horas-complementares.test.ts:38` «250 permanece bruto» e `:71` «250 → 240» (critério 9) | **COMPROVADA** |
| R5 | P-03 (RN-412) | `api/tests/extrato.test.ts:483` «total 1420, aproveitado 1200» (critério 8); `api/tests/horas-complementares.test.ts:89` «teto 1200» | **COMPROVADA** |
| R6 | P-04 (RN-407) | `api/tests/gerar-codigo.test.ts:8` «prefixo `SA26-`», `:18` «blocos 4+4», `:91` «50 códigos únicos» (critérios 11–12); `api/tests/certificado-colisao.test.ts:115` «colisão → nova tentativa, unicidade no repositório» | **COMPROVADA** |
| R7 | P-05 (RN-408/409) | `api/tests/abreviar-nome.test.ts:6` «Carla M. S.», `:10` «Elisa F. da Rocha», `:14` «Isadora R. dos Santos» (critérios 13–14) | **COMPROVADA** |
| R8 | P-05 (RN-408/409) | `api/tests/certificado-r8.test.ts:83` «sem `X-Usuario` → exatamente os 5 campos públicos», `:112` «não expõe `participanteId`/presencas/encontros» (critérios 15–16) | **COMPROVADA** |
| R9 | P-06 (RN-407) | `api/tests/certificado.test.ts:176` «201 e depois 200 com mesmo `codigo` e `emitidoEm`»; `api/tests/certificado-r9.test.ts:79` «reutiliza certificado, COUNT=1» (critério 17) | **COMPROVADA** |
| R10 | P-07 (RN-410) | `api/tests/extrato.test.ts:104` «`codigo` preenchido e `codigo: null`»; `:172` «recalculado a cada consulta» (critério 18) | **COMPROVADA** |
| R11 | P-08 (RN-407/RN-302) | `api/tests/gerar-codigo.test.ts:36-81` «sem I, O, 0, 1» + regex `SA26-[A-HJ-NP-Z2-9]` (critério 19) | **COMPROVADA** |
| R12 | P-09 (RN-413) | `api/tests/certificado-r12.test.ts:75` «422 `ATIVIDADE_NAO_ENCERRADA` e não `PRESENCA_INSUFICIENTE`» (critério 20) | **COMPROVADA** |
| R13 | P-10 (RN-410/403) | `api/tests/extrato.test.ts:104-168` «confirmada aparece; sem certificado → `codigo: null`» (critério 21) | **COMPROVADA** |
| R14 | P-11 (RN-410/403) | `api/tests/extrato.test.ts:219` «`em_espera` não aparece» (critério 22) | **COMPROVADA** |
| R15 | P-12 (RN-410/403/214) | `api/tests/extrato.test.ts:248` «`convocada` não aparece» (critério 23) | **COMPROVADA** |
| R16 | P-13 (RN-410/403/210) | `api/tests/extrato.test.ts:288` «`cancelada` não aparece» (critério 24) | **COMPROVADA** |
| R17 | P-14 (RN-410/403/210) | `api/tests/extrato.test.ts:319` «`expirada` não aparece» (critério 25) | **COMPROVADA** |
| R18 | P-15 (RN-402/410) | `api/tests/extrato.test.ts:362` «atividade cancelada não aparece» (critério 26) | **COMPROVADA** |

**Resumo da matriz:** 18 de 18 regras COMPROVADAS; 0 SEM ORIGEM; 0 SEM PROVA; 0 PROVA FRACA; 0 NÃO CONTRATADO. Os 26 critérios de aceite da spec estão cobertos pelos testes citados (critérios 1–6 em `certificado.test.ts`; 7–10 em `extrato.test.ts` + `horas-complementares.test.ts`; 11–12 em `gerar-codigo.test.ts` + `certificado-colisao.test.ts`; 13–14 em `abreviar-nome.test.ts`; 15–16 em `certificado-r8.test.ts`; 17 em `certificado.test.ts` + `certificado-r9.test.ts`; 18–26 em `extrato.test.ts`).

---

## Seção 3 — API

- **Camadas separadas** conforme `AGENTS.md`: HTTP em `api/src/app.ts:394-460`; aplicação/domínio em `api/src/certificate/`; persistência em `api/src/repositories/certificate-repository.ts`; integração com outros módulos apenas por portas explícitas `api/src/integrations/m2-port.ts` (M2) e `api/src/integrations/m3-presence-port.ts` (M3) — nenhum acesso direto a detalhes internos de M1/M2/M3.
- **Rotas M4** (todas com camada HTTP fina, delegando à aplicação):
  - `POST /atividades/:id/certificado` — `api/src/app.ts:394` (`requireUser`; `:397` rejeita não-participante com `403 SOMENTE_PARTICIPANTE`; `:429` devolve `201` criado / `200` reutilizado).
  - `GET /certificados` — `api/src/app.ts:432` (`requireUser`, `requireParticipant`; `:434-435` repositório → JSON).
  - `GET /extrato` — `api/src/app.ts:438` (`requireUser`, `requireParticipant`).
  - `GET /certificados/:codigo` — `api/src/app.ts:443` (público, sem `requireUser`; `:445` 404 quando não existe; `:450-458` monta os campos públicos).
- **Persistência:** tabela `certificados` criada em `api/src/db/migrate.ts:61`; seed limpa a tabela em `api/src/db/seed.ts:58`; repositório com `create` (`certificate-repository.ts:24-31`) e consultas por participante/atividade+participante/código (`:37`, `:69`, `:95`).
- **Relógio:** nenhuma regra temporal do M4 lê a hora do sistema diretamente; a emissão usa o relógio injetado (`api/src/certificate/emissao-certificado.ts` recebe `ControllableClock`), e os testes controlam o instante via `PUT /_teste/relogio` (rotas `/_teste/*` condicionadas a `MODO_TESTE=1`).
- **Erros:** formato `{ erro, mensagem }` conforme contrato (ex.: `SOMENTE_PARTICIPANTE`, `ATIVIDADE_NAO_ENCERRADA`, `PRESENCA_INSUFICIENTE`).

---

## Seção 4 — Certificados (emissão e listagem)

- **Regras cobertas:** R1 (75% mínimo, sem arredondamento), R2 (atividade encerrada), R9 (idempotência por atividade+participante), R12 (prioridade do código de erro quando a atividade não terminou).
- **Domínio:** `api/src/certificate/emissao-certificado.ts` — valida presença, participação e encerramento; `:68` devolve o certificado existente com `criado: false` (idempotência); `:77-88` cria registro com `codigo` e `emitidoEm` do relógio injetado; `:97` estoura erro após `LIMITE_DE_TENTATIVAS` sem código único.
- **Testes da API que comprovam:**
  - `api/tests/certificado.test.ts:82,98,112,126` (R1), `:144,158` (R2), `:176` (R9).
  - `api/tests/certificado-r9.test.ts:79` (reutilização preserva código, COUNT=1).
  - `api/tests/certificado-r12.test.ts:75` (erro correto quando não terminou).
  - `api/tests/certificado-colisao.test.ts:115` (colisão de código → nova tentativa).
  - Complementares lidos: `certificado-nao-inscrito.test.ts`, `certificado-atividade-cancelada.test.ts`, `certificado-g3-presenca-manual.test.ts`, `certificado-listagem.test.ts`, `certificado-contrato-http.test.ts` (forma HTTP dos erros e do corpo).
- **Situação de inscrição:** a ausência de inscrição é recusada pelo caminho de participação (`403 SOMENTE_PARTICIPANTE` em `api/src/app.ts:397`), coberta por teste.

---

## Seção 5 — Extrato de horas

- **Regras cobertas:** R3 (somatórios brutos), R4 (teto de 240 para palestras), R5 (teto global de 1200 no aproveitado), R10 (`codigo` preenchido/nulo e recálculo por consulta), R13–R18 (filtros por situação de inscrição, expiração e cancelamento de atividade).
- **Domínio:** `api/src/certificate/extrato.ts` — `:41` busca o certificado por atividade+participante e `:47` devolve `codigo: certificado ? certificado.codigo : null`; `api/src/certificate/horas-complementares.ts` aplica brutos e tetos (240 e 1200) como funções puras testáveis.
- **Testes da API que comprovam:**
  - R3: `api/tests/extrato.test.ts:392` + `api/tests/horas-complementares.test.ts:45`.
  - R4: `api/tests/extrato.test.ts:445` + `api/tests/horas-complementares.test.ts:38,71`.
  - R5: `api/tests/extrato.test.ts:483` + `api/tests/horas-complementares.test.ts:89`.
  - R10: `api/tests/extrato.test.ts:104,172`.
  - R13: `api/tests/extrato.test.ts:104-168`; R14: `:219`; R15: `:248`; R16: `:288`; R17: `:319`; R18: `:362`.
- **Honestidade das provas:** cada teste monta a situação que dispara a condição (inscrição `em_espera`/`convocada`/`cancelada`/`expirada`, atividade cancelada), faz a chamada real a `GET /extrato` e verifica ausência/presença no JSON — não apenas o status.

---

## Seção 6 — Verificação pública

- **Regra coberta:** R8 (expostos exatamente os 5 campos públicos, sem `X-Usuario`), além das regras de código R6 (prefixo/blocos/unicidade) e R11 (alfabeto sem ambíguos).
- **Rota:** `GET /certificados/:codigo` — `api/src/app.ts:443` sem `requireUser`; `:445` 404 se não existe; `:450-458` monta `codigo`, `atividade`, `participante` (nome abreviado), `cargaHorariaMinutos`, `emitidoEm`.
- **Domínio:** `api/src/certificate/abreviar-nome.ts` (abreviação de nomes do meio com particle `da/de/do`); `api/src/certificate/gerar-codigo.ts` — `ALFABETO = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'` (32 chars) + `randomBytes(8)` com `b % 32`; **256 % 32 = 0 → sem viés de módulo** (viés avaliado e descartado comprovadamente, não é achado).
- **Testes que comprovam:**
  - R8: `api/tests/certificado-r8.test.ts:83` (sem `X-Usuario` → exatamente 5 campos), `:112` (não vaza `participanteId`/presencas/encontros).
  - R7/R13/R14 da abreviação: `api/tests/abreviar-nome.test.ts:6,10,14`.
  - R6: `api/tests/gerar-codigo.test.ts:8,18,91` + `api/tests/certificado-colisao.test.ts:115`.
  - R11: `api/tests/gerar-codigo.test.ts:36-81` (sem `I`, `O`, `0`, `1`; regex `SA26-[A-HJ-NP-Z2-9]`).

---

## Seção 7 — Interface

- **Rotas** (`app/src/App.tsx`): `/certificados` (`:224`), `/extrato` (`:235`), `/verificar` (`:246` e rota pública `:335`). Links de navegação em `app/src/components/Programacao.tsx:58,64,73`.
- **Componentes:** `app/src/components/MeusCertificados.tsx` (loading `:45`, erro `:51`, vazio `:61`, lista `:65-71` com `data-testid="meus-certificados-container"`), `app/src/components/ExtratoHoras.tsx`, `app/src/components/VerificarCertificado.tsx` (campo `:69-73`, erro `:110`, container `:120-124`).
- **Hooks:** `app/src/hooks/useCertificados.ts`, `app/src/hooks/useExtrato.ts`, `app/src/hooks/useCodigoEncontro.ts` (linha 12 = baseline do M3, ver Seção 8).
- **Cliente HTTP:** `app/src/api/client.ts:176` (`GET /certificados`), `:180` (`GET /extrato`), `:184` (`GET /certificados/${encodeURIComponent(codigo)}`). O cliente normaliza o código com `trim().toUpperCase()` e o servidor faz busca exata — coerente entre camadas, **não há divergência comprovada** a registrar.
- **Testes da interface (com API falsa/mock, conforme `AGENTS.md`):**
  - `app/src/components/MeusCertificados.test.tsx:22,42` (loading), `:64` (lista), `:75,90` (vazio), `:97,114` (erro).
  - `app/src/components/ExtratoHoras.test.tsx` (somatórios, tetos, filtros).
  - `app/src/components/VerificarCertificado.test.tsx:32,55,83,87` (sucesso), `:116,127,141,146` (erro), `:164,170` (container).
  - `app/src/api/client.test.ts:266-277` (`GET /certificados`), `:281-317` (`GET /extrato`), `:321-359` (`GET /certificados/:codigo` com código codificado na URL e normalização).
  - `app/src/App.test.tsx:1658,1684,1714,1727` (rotas `/certificados`, `/extrato`, `/verificar` com e sem participante).
- **Escopo da interface:** não existe tela/botão de emissão (UI-6) — a spec não exige rota da interface para emissão; **a ausência não é classificada como falha** por não haver regra da spec que a exija.
- **Warnings:** a execução emite avisos `act(...)` no stderr de `App.test.tsx` (dependências de outros módulos); todos os testes passam — não é falha.

---

## Seção 8 — Testes (números exatos)

| Verificação | Comando | Resultado exato da saída |
|---|---|---|
| Suíte da API | `npm --prefix api test` | **Test Files 38 passed (38); Tests 271 passed (271)** (~11.7s) |
| Suíte da interface | `npm --prefix app test` | **Test Files 10 passed (10); Tests 136 passed (136)** (~11.5s) |
| Typecheck da interface | `npx tsc --noEmit` (em `app/`) | **1 erro**: `src/hooks/useCodigoEncontro.ts(12,29): error TS2503: Cannot find namespace 'NodeJS'.` (baseline do M3, commit `111f163`) |
| Typecheck da API | `npx tsc --noEmit` (em `api/`) | **7 erros**: `Property 'close' does not exist on type 'Express'` em `api/tests/cancel-activity-effects.test.ts:52`, `api/tests/cancel-activity.test.ts:226`, `api/tests/update-activity.test.ts:502,541,586,625,664` (baseline do M1, commits `69fcce4`/`465bcca`/`6f335df`) |
| Build da interface | `npm --prefix app run build` (`tsc && vite build`) | **Falha** pelo mesmo erro baseline de `useCodigoEncontro.ts(12,29)` |
| Cobertura do `projeto.json` | leitura de `projeto.json` | `testes` = apenas os dois comandos vitest acima; `typecheck` existe nos `package.json` mas **não** está no `projeto.json` |

Números acima copiados da saída dos comandos; nenhum número repetido sem ter sido visto na execução. Os erros de typecheck são pré-existentes de M1/M3 e **não bloqueiam** `npm --prefix api test` (vitest não roda typecheck); foram registrados à parte, sem correção.

---

## Seção 9 — Qualidade dos testes

- **Sem prova fraca:** em cada regra o teste executa o cenário real e verifica o resultado exigido — status **e** código de erro **e** valores (ex.: `extrato.test.ts:445` verifica `palestrasMinutos: 300` bruto **e** aproveitado `240`, não só `200`; `certificado-r8.test.ts:83` verifica o conjunto exato de 5 campos, não apenas `403`).
- **Relógio de teste:** os testes temporais (R2 — instante exato do fim) usam o relógio controlável (`ControllableClock` via `/_teste/relogio`), nunca o relógio real.
- **Dados que disparam a condição:** cada filtro do extrato monta a inscrição/situação que aciona a regra (não usa dado inerte).
- **Interface:** todos os testes usam cliente falso (`fakeClient`) — nenhuma chamada a API real.
- **Nenhum teste existente foi alterado apenas para passar** (verificado no histórico de commits do módulo).
- **Testes M4 da API lidos:** `certificado.test.ts`, `certificado-r8/r9/r12.test.ts`, `certificado-colisao.test.ts`, `certificado-nao-inscrito.test.ts`, `certificado-atividade-cancelada.test.ts`, `certificado-g3-presenca-manual.test.ts`, `certificado-listagem.test.ts`, `certificado-contrato-http.test.ts`, `extrato.test.ts`, `horas-complementares.test.ts`, `gerar-codigo.test.ts`, `abreviar-nome.test.ts`.

---

## Seção 10 — Evidências

- **`evidencias/sessoes/enzofenato/INDICE.md:15-28`:** 14 sessões (`json`+`md`), última em **16/09 11:29**.
- **Lacuna:** os commits da interface do M4 (`e0b601b` M4-UI1 RED, 21/09 18:51, até `63d10d3` 22/09 08:31 — M4-UI1..M4-UI5 e ajuste da skill) são **posteriores** ao último export; as sessões da interface do M4 **não estão exportadas**.
- **Demais índices lidos:** `evidencias/sessoes/pedro/INDICE.md`, `evidencias/sessoes/fernando/INDICE.md`, `evidencias/sessoes/lucas-jose-gomes-oliveira/INDICE.md`.
- **`evidencias/RESUMO.md`:** não existe; só é gerado pela flag `--resumir` (`evidencias/exportar-evidencias.js:605-607`).
- **Correção mínima (não executada — auditoria é somente leitura):** `node evidencias/exportar-evidencias.js` e `node evidencias/exportar-evidencias.js --resumir`.

---

## Seção 11 — Git / escopo

- **Árvore de trabalho:** a auditoria não modificou nenhum arquivo.
- **Escopo fora de contratar (§2 da spec) — nenhum código proibido encontrado:**
  - Sem geração de PDF (componentes só renderizam HTML).
  - Sem implementação de QR de encontro (`GET /encontros/:id/codigo` é de outro módulo; M4 só consome).
  - Sem rotas de autenticação/cadastro no M4.
  - Sem revogação/cancelamento de certificado (não previsto na spec).
  - Sem recálculo de situação temporal com hora do sistema (relógio central injetado).
  - Integrações apenas pelas portas `m2-port.ts` e `m3-presence-port.ts`.
- **Divergência de contrato:** nenhuma divergência comprovada de rota, campo ou código de erro (normalização de código cliente/servidor é coerente) — nada a encaminhar ao revisor de contrato.
- **Viés de módulo em `gerar-codigo.ts`:** avaliado e **descartado** (32 caracteres, `256 % 32 = 0`) — não é achado.
- **Saída final de `git status --short`:** **vazia** — branch `feat/m4-certificados`, atualizada com `origin`, `nothing to commit, working tree clean`.

---

## Seção 12 — Achados classificados

**Contagem: 0 BLOQUEADOR · 0 IMPORTANTE · 0 MENOR · 4 OBSERVAÇÃO · 0 SEM PROVA · 0 PROVA FRACA · 0 SEM ORIGEM · 0 NÃO CONTRATADO.**

1. **[OBSERVAÇÃO] Evidências do M4 desatualizadas.**
   - Regra/critério afetado: nenhuma regra funcional; lacuna documental do módulo (Seção 10 do relatório).
   - Arquivo: `evidencias/sessoes/enzofenato/INDICE.md:15-28`.
   - Teste relacionado: não aplicável (evidência de sessão, não de código).
   - Evidência: índice para em 16/09 11:29; commits M4-UI de 21–22/09 (`e0b601b`..`63d10d3`) ausentes do export.
   - Correção mínima: rodar `node evidencias/exportar-evidencias.js` e `--resumir` (não executada pela auditoria).

2. **[OBSERVAÇÃO] Baseline de typecheck da interface (fora do M4).**
   - Arquivo: `app/src/hooks/useCodigoEncontro.ts:12` (`useRef<NodeJS.Timeout | null>` → `TS2503: Cannot find namespace 'NodeJS'`).
   - Teste relacionado: derruba `npx tsc --noEmit` e `npm --prefix app run build`; não derruba `npm --prefix app test`.
   - Evidência: arquivo do M3 (commit `111f163`), não introduzido pelo M4.
   - Correção mínima (para o dono do M3): tipar o retorno com `ReturnType<typeof setTimeout>` em `useCodigoEncontro.ts:12`.

3. **[OBSERVAÇÃO] Baseline de typecheck da API (fora do M4).**
   - Arquivo: 7 erros `.close()` em `Express` — `api/tests/cancel-activity-effects.test.ts:52`, `api/tests/cancel-activity.test.ts:226`, `api/tests/update-activity.test.ts:502,541,586,625,664`.
   - Teste relacionado: `npm --prefix api test` (vitest) passa `271/271` por não rodar typecheck.
   - Evidência: testes do M1 (commits `69fcce4`/`465bcca`/`6f335df`).
   - Correção mínima (para o dono do M1): usar o tipo do `server`/`app` com `close` (ex.: `Server` do `http`) nesses testes.

4. **[OBSERVAÇÃO] `evidencias/RESUMO.md` inexistente.**
   - Arquivo: raiz de `evidencias/` (só `exportar-evidencias.js`).
   - Evidência: resumo é gerado somente com `--resumir` (`evidencias/exportar-evidencias.js:605-607`).
   - Correção mínima: rodar `node evidencias/exportar-evidencias.js --resumir`.

**Nenhum achado de código do M4:** as 18 regras e os 26 critérios têm teste honesto; nenhum comportamento fora do escopo da spec foi encontrado nas rotas (`api/src/app.ts:394,432,438,443`) nem no domínio lido.

---

## Seção 13 — Parecer final

Conferi **18 regras (R1–R18) uma a uma**, com origem em entrevista respondida e teste comprobatório honesto, mais os 26 critérios de aceite. Suítes verdes com números exatos (**API 271/271 em 38 arquivos; interface 136/136 em 10 arquivos**). Os únicos pontos negativos são observações de documentação (evidências/`RESUMO.md`) e dois baselines pré-existentes de outros módulos (M1 e M3) fora do escopo desta auditoria.

**Classificação exata: APROVADO COM RESSALVAS**

Ressalvas: (1) reexportar as sessões da interface do M4 nas evidências (`evidencias/exportar-evidencias.js`, opcionalmente `--resumir`); (2) os problemas de typecheck pré-existentes do M3 (`useCodigoEncontro.ts:12`) e do M1 (7 erros `.close()` em `Express`), registrados à parte por serem de outros módulos, que impedem `npm --prefix app run build` e `npx tsc --noEmit` — nenhum deles introduzido pelo M4.

**Saída final de `git status --short`:**

```
```

(vazia — `feat/m4-certificados`, `nothing to commit, working tree clean`; a auditoria não alterou nenhum arquivo.)